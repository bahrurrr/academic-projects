#include <stdio.h>
#include <math.h>

int main ()
{
    float y1, y2, b, x, c, z, a;

    //rumus pertama
    y1 = b * pow(x,2) + 0.5 * x - c;

    //rumus kedua
    y2 = (0.3 * x * z) / (2 * a);

    printf ("hasil y1 = %lf\n", y1);
    printf ("hasil y2 = %lf\n", y2);

    return 0;
}